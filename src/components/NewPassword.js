import React from "react";

const NewPassword = () => {
  return (
    <div className="container">
      <p>
        Your password has been successfully changed. Click "Close" to return to
        the Login Page.
      </p>
    </div>
  );
};

export default NewPassword;
